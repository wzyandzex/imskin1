# IMSkin 架构设计

> 版本：v3.0 ｜ 状态：设计基线 ｜ 最后更新：2026-08-15
>
> v3.0 变更：与总规划对齐——出口交付等级、TargetVariant 模型、分层 QA Gate、本地任务生命周期、LLM 安全边界；**删除百度 PC"驱动官方编辑器"旧路线**（历史方案见 `docs/archive/2026-08-15/`）；逆向证据保留为 E2 级事实引用，不再当作安装证明。
> 详细契约在 `docs/01`（领域）/`docs/02`（平台）/`docs/03`（质量）/`docs/04`（工程），本文只定边界与数据流。

---

## 0. 硬性原则（一票否决）

| 被否决方案 | 理由 |
|---|---|
| 静态效果图当皮肤 | 用户拿不到可用产物 |
| 录屏/截图轮播当预览 | 无法暴露真实排版边界 |
| 逆向猜"假格式"当已验证 | 客户端升级即碎；必须按证据等级管理（`docs/05`） |
| 大模型端到端吐文件 | 无法局部重跑/定位 |
| 写死候选词 | 测不出超长/生僻/翻页场景 |
| 一套逻辑打两个平台 | 预览与真实客户端有出入（PRE-001 即修复此违规） |
| 非空 ZIP 当可安装包 | 只有 E4 真机证据可升级 install_verified |

## 1. 系统总览

```text
① 输入（想法/图/视频/语音） → ② Brief 确认（hash） → ③ VisualSpec + ElementCatalog
→ ④ AssetBundle（设计层 + 四 TargetVariant 资产） → ⑤ 分层 QA Gate（G1–G3）
→ ⑥ 按 outlet 适配打包（G4–G5） → ⑦ 浏览器真实交互（G6）
→ ⑧ 用户确认此版本 → ⑨ 导出 Artifact（install_candidate）
→ ⑩ 真机安装 Evidence（G7）→ install_verified
反馈环：⑦ 处反馈 → ChangeInstruction → 定向重跑 ③/④/⑥ 受影响层 → fork 新版本
```

## 2. 分层架构

```text
┌ 前端平台（web-app）
│   输入 UI / Brief 卡片 / 工作台（版本树+预览+反馈）/ 设置
│   PreviewRuntime：PC + Android 两套交互层，共享渲染基座
│   · 输入内核：pinyin-engine（26 键 + T9）
│   · 皮肤渲染：SkinManifest/Fill/NineSlice（Canvas+DOM）
│   · 按 outlet 加载 PreviewProfile（平台差异唯一入口）
├ 智能体编排（orchestrator）
│   A1 意图 → A2 视觉 → A3 资产 → A4 装配 → A5 反馈 → A6 QA
│   · 按 TargetVariant 定向重跑；OutletJob 生命周期；provenance
├ 领域内核（零依赖核心包）
│   skin-gen（Brief/Spec/SkinManifest） · project-model（版本树）
│   feedback-core（分类/路由/ChangeInstruction） · qa-core（Gate 检查器）
│   contracts（跨包类型 + 运行时 parser，DOM-001 新增）
├ 平台适配（四 adapter，统一 PlatformAdapter 端口）
│   sogou-adapter / sogou-mobile-adapter / baidu-pc-adapter / baidu-mobile-adapter
│   · capabilities / assetProfile / previewProfile / build / validateArtifact / installVerificationProfile
├ 支撑层
│   zip（零依赖容器） · llm-core（可选 provider 注册 + 降级链）
│   api（本地 REST/CLI/SDK；LLM 代理目标，ADR-005）
└ 数据与状态
    Project / Version（生命周期） / DesignSnapshot（不可变）
    AssetBundle / TargetVariant（交付等级） / OutletJob / PackageArtifact / QAReport / Evidence
```

**依赖方向**（`docs/04` §3）：contracts ← 领域内核/adapter ← orchestrator ← api/web-app；禁止反向与成环。

## 3. 关键架构决策（ADR 索引）

| ADR | 决策 |
|---|---|
| ADR-001 | 出口五级交付状态，独立推进/失败/重试 |
| ADR-002 | 设计层共享 + 每版本四 TargetVariant |
| ADR-003 | contracts 包 + 稳定 element_id 命名空间 |
| ADR-004 | G0–G8 分层 QA Gate，状态由报告聚合 |
| ADR-005 | LLM 密钥会话化/本地代理优先 + SSRF 边界 |
| ADR-006 | OutletJob 本地受控任务模型 |
| ADR-007 | 搜狗 PC 黄金出口，移动预览并行 |
| ADR-008 | 资源缺失诚实降级，禁止假占位 |

## 4. 四出口生成策略（现状=编程直生，待 E4 收口）

| 出口 | 容器/结构 | 证据等级 | 关键未决 |
|---|---|---|---|
| 搜狗 PC .ssf | zip + skin.ini(UTF-16LE)；加密容器子路径备用 | E2 | 明文 zip 接受性、边缘字段（R-01） |
| 搜狗 Android .ssf | 普通 zip：phoneTheme.ini + theme/<layout> + res/resblack | E2 | 布局键名/取值、DPI（R-04） |
| 百度 PC .bps | zip：skin.ini(UTF-16LE)+Skin.xml+Candidate/Status.xml+位图 | E2 | store vs deflate、metadata 规则（R-02） |
| 百度 Android .bds | zip：Info.txt+Token.txt+port/land+css.ini(+keyshape) | E2 | Token MD5 语义、后缀、登录校验（R-03） |

生成策略统一为：**格式公开可写则编程直生（首选）**；当前四出口均已按此落地为 structural 实现。逆向结论（open-reverselab 笔记、ssfconv、BGtool.c 等开源旁证）支撑字段建模，但**任何出口的"可安装"必须 E4 真机证据**（`docs/05` §6 manifest）。

## 5. 预览引擎

- 双运行时：PC（26 键/横竖候选/状态栏）与 Android（T9/26 键/手势/音效/DPI），共享皮肤渲染与版本加载基座，交互层独立（ADR 索引同旧版 ADR-8 思想）。
- **PreviewProfile 是平台差异唯一入口**：候选行为、功能栏、能力开关、模拟项标注（"浏览器模拟，真机更强"）。
- 输入内核 pinyin-engine：真实拼音/T9；词库经 PINYIN-001 benchmark 达标，否则 UI 标注"简化预览词库"。
- 性能预算：按键→候选 ≤50ms（P95）、音效 ≤30ms、动效 ≥50fps、版本切换 ≤1s（协议见 `docs/03` §13）。
- 分层渲染：背景层（图/渐变/九宫格 Canvas）→ 按键层（DOM 交互）→ 特效层（Canvas，pointer-events:none）→ 候选/工具栏层（DOM）。

## 6. 反馈与版本

- A5 输出 ChangeInstruction（目标 element_id/字段路径/操作/保留集/置信度/目标出口）；置信度 <0.7 先请用户确认目标。
- 平台反馈只重跑对应 TargetVariant（token diff 证明）；通用风格反馈同步设计层后四端重适配。
- 版本树：fork/lineage/元素合并（结构化 element path + MergeRecord）；显式满意=硬保护，推断满意=软偏好。
- 生命周期与出口交付等级两轴分离（`docs/01` §5/§9）。

## 7. QA 与发布

分层 Gate G0–G8（`docs/03`）：确认→schema→资产→渲染→适配→一致性→浏览器→真机→性能/安全。状态由报告聚合计算，UI 不得手写布尔。一键修复产生版本节点；image 可读性必须渲染后采样。

## 8. 任务与 API

- OutletJob：每出口一个 job（stage/progress/attempt/TTL/cancel/retry），本地受控实现、接口可替换（ADR-006）。
- api 包：DTO/认证/任务查询/产物下载 + LLM 本地代理（中期）；非 loopback 强制 key；产物带 artifactId/hash/deliveryLevel/experimental 元数据。

## 9. 数据与存储

- DesignSnapshot 不可变、带 schemaVersion 与迁移（旧 `.imskin.json` round-trip）。
- 资产二进制与元数据分离（content hash 引用）；本地 localStorage 过渡 → IndexedDB（大项目）→ 对象存储（云端期）。
- provenance 记录 stage/输入 hash/实现版本/依赖/seed/fallback；时间与 GUID 可注入以保证可复现构建。

## 10. 风险与技术验证

风险台账统一在 `docs/05`（R-01..R-16 + provisional 锚点表）。当前最高优先外部验证：

1. R-01 搜狗 PC 真机安装（EVID-001，黄金出口）；
2. R-02 百度 PC 双包对照实验；
3. R-03 百度 Android Token 实验矩阵；
4. R-04 搜狗 Android 布局样本核对；
5. D2 词库选型 benchmark。

## 11. 追溯

本文 §2 分层 ↔ `docs/01` 领域；§4 出口 ↔ `docs/02` 矩阵；§7 QA ↔ `docs/03`；工程落地 ↔ `docs/04`/`docs/06`；需求 ↔ 《需求规格说明书.md》v1.3 §8 矩阵。
