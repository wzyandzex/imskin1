# 输入法皮肤智能体系统 — 需求规格说明书（SRS / PRD）

> 版本：v1.3 ｜ 状态：需求基线 ｜ 最后更新：2026-08-15
>
> v1.3 变更要点：① 交付承诺改为**每出口独立交付等级**（structural / previewable / install_candidate / install_verified，定义见 `docs/00` §1），废弃"四出口同时可安装"的一揽子表述；② "移动端"统一为 **Android**（iOS 无外部导入通道，仅设计稿边界）；③ 百度 PC 路线按逆向证据改为**编程直生**（真机验证未完成）；④ 补齐量化验收（对比度、性能统计口径、输入边界、反馈不变证明）；⑤ LLM/自动化 API 定位为**可选扩展**并纳入安全需求。
> 历史版本见 `docs/archive/2026-08-15/`。

---

## 1. 引言

### 1.1 目的

规定系统**做什么**与**做到什么程度**。任何实现是否合格，以本文验收标准（AC）与 `docs/03-质量门禁与验收协议.md` 的 Gate 报告为准。

### 1.2 产品定义

用户用一段想法（可带参考图/视频）生成输入法皮肤设计；在网页里像真实输入法一样试打预览；用自然语言或元素标注做多轮局部修改；版本树保留探索过程；用户明确确认后按出口导出皮肤包。

**对外承诺随出口交付等级变化**：只有 `install_verified` 的出口可称"可安装包"；其余等级必须如实标注（实验包/预览/待验证）。

### 1.3 产品原则（一票否决线）

1. **真实体验优先**：不接受静态图、录屏、截图轮播、写死候选词、换皮套壳、非空 ZIP 冒充可安装包。
2. **诚实呈现**：平台差异、降级、模拟、未验证能力必须明示；不允许用统一步调掩盖分出口进度差异。
3. **局部反馈可解释**：改了什么、保护了什么、哪个出口被重跑，必须可被 diff 证明。

### 1.4 优先级

| 级别 | 含义 |
|---|---|
| P0 | 黄金出口闭环必需，缺失则产品不成立 |
| P1 | 核心体验完整所必需，首版强烈建议 |
| P2 | 增强项，后续迭代 |

---

## 2. 范围

### 2.1 范围内

- 四个**渐进交付**出口：搜狗 PC（.ssf）、搜狗 Android（.ssf）、百度 PC（.bps）、百度 Android（.bds）。
- 完整生成管线：想法 → Brief 确认 → VisualSpec → 资产 → 适配 → QA → 导出。
- 自研可交互预览运行时（PC + Android 两套交互层，共享渲染基座；含真实拼音/T9 引擎）。
- 多轮反馈（自然语言 + 元素标注）+ 版本树 + 元素合并。
- 深浅双模式、多档 DPI 预览。
- 项目文件导出/导回。
- 可选：LLM 接入（意图理解/反馈增强/图像生成）、本地 REST/CLI/SDK。

### 2.2 范围外（本期）

- 搜狗/百度之外的输入法平台；iOS 可导入皮肤包（沙盒限制，仅出设计稿）。
- 皮肤商店/社区分发/账号商业化。
- 云端多租户、MCP、跨项目偏好（另立 PRD/ADR 后启动，见 `docs/00` §7 Phase 7）。

### 2.3 关键依赖与假设

| # | 假设 | 状态 | 管理 |
|---|---|---|---|
| D1 | 四出口格式可编程直生 | 逆向证据支持（E2 级） | `docs/02` §5–8，逐出口真机收口 |
| D2 | 存在可用开源词库满足日常输入 | 待选型 | PINYIN-001 + benchmark 门槛 |
| D3 | 图像生成可用（用户 endpoint 或服务） | 可选依赖 | 缺失时按 ADR-008 诚实降级 |
| D4 | 主流客户端接受生成包 | **未验证** | R-01..R-05，E4 证据前禁止"可安装"表述 |

---

## 3. 用户与场景

### 3.1 用户画像（按优先级）

1. **P-普通创作者**（首发）：不懂设计工具，说想法就要能用的皮肤，能试打、能聊天式修改。
2. **P-有想法用户/设计师**：有参考图与明确审美，要还原度、局部精修、已满意处不被改坏。
3. **P-内部兜底人员**：处理适配/真机/外部工具失败（内部角色，不是公开工作台主用户）。

### 3.2 核心用户故事

- US-1 一句话生成可用的皮肤设计并真实试打。
- US-2 上传参考图，提取配色/风格融入设计。
- US-3 "候选词字太小"——只改这一点，其他不动（可被 diff 证明）。
- US-4 "回到第 2 版的感觉，候选栏用第 4 版的"（版本树 + 元素合并）。
- US-5 我明确点"确认此版本"后才导出；系统不擅自判定"改好了"。
- US-6 手机上试九宫格、按键音、上滑，判断装到真机的样子（模拟项明示）。
- US-7 各出口独立进度，快的先体验，失败的看到卡点和重试。
- US-8 深浅双模式自动携带且两套都通过可读性检查。
- US-9 分享/导回项目文件继续编辑。

---

## 4. 功能需求

> 每条含 ID、优先级、AC。标 ❌ 为不可接受实现。**交付等级相关的 AC 以 Gate 报告为准**（`docs/03`）。

### 4.1 FR-INPUT 输入采集

**FR-INPUT-1（P0）想法与素材**
- AC1 文字必填；图片支持 PNG/JPEG/WebP，≤10MB，最长边 ≤4096；视频支持 MP4/WebM，≤100MB、≤60s（超限明确报错，不静默降质）。
- AC2 参考图提取主色/构图密度/材质倾向/圆角/字重；视频抽关键帧（≤3 帧）提取同项。
- AC3 视频动效意向识别为**行为性需求**路由动效设计；未实现前明确标注"动效意向暂未支持"，不冒充已理解。

**FR-INPUT-2（P0）克制式追问**
- AC1 风格/配色/情绪缺两项以上时触发；一次最多一问；可推断不问。❌ 问卷式连续追问。
- AC2 "你看着办"跳过并在 Brief 标注推断字段。

**FR-INPUT-3（P1）照片一键成皮 [调研]**
- AC1 与文字通道同构（Brief→生成→预览→迭代），❌ 一次性贴图。
- AC2 自动融合保底（不挡字、对比度 ≥4.5）。
- AC3 无完整资产能力前不作为正式发布阻塞；入口需标注当前产物等级。

**FR-INPUT-4（P1）语音输入**
- AC1 想法与反馈框均有麦克风；转写可编辑后提交；同链路处理不降级。
- AC2 不支持/拒绝权限时明确提示并回退文字。❌ 静默失败。

### 4.2 FR-BRIEF 设计意图

**FR-BRIEF-1（P0）结构化 Brief**
- AC1 至少含：风格关键词、配色、情绪、材质方向、圆角、动效意向、素材引用、目标出口、推断字段标注（JSON Pointer）。

**FR-BRIEF-2（P0）确认关卡**
- AC1 可编辑卡片；推断字段高亮；确认后记录 Brief hash。
- AC2 Brief 变更使确认失效。❌ 跳过确认直接跑全管线。

### 4.3 FR-GEN 视觉与资产生成

**FR-GEN-1（P0）VisualSpec + 元素目录**
- AC1 token 含颜色/间距/圆角/字重；每个可编辑元素有稳定 `element_id`（命名空间见 `docs/01` §7.1）。

**FR-GEN-2（P0）双层资产：设计层 + 四 TargetVariant**
- AC1 设计层一份（主视觉/色板/字体/动效脚本/移动音效触感意向）。
- AC2 四出口各持有独立 AssetSet（布局、状态图、DPI、音效、动效）；任一出口重跑不影响其余（token diff 可证）。
- AC3 资产自检：尺寸匹配规格、九宫格安全区、帧数在平台支持范围。

**FR-GEN-3（P1）图像生成质量**
- AC1 采用可产出高质量成品的方案；失败按 ADR-008 降级（确定性替代或缺列 missing），❌ 假图占位/静默缺图。

### 4.4 FR-PKG 打包与四出口

**FR-PKG-1（P0）统一适配器契约**
- AC1 每出口适配器暴露 capabilities/assetProfile/previewProfile/build/validateArtifact/installVerificationProfile（`docs/02` §3）。
- AC2 构建失败明确报告卡点，❌ 静默产假文件。

**FR-PKG-2（P0）出口交付等级**
- AC1 每出口独立呈现 structural→previewable→install_candidate→install_verified 进度与缺口。
- AC2 升级必须引用 Evidence；客户端大版本升级后 E4 证据失效需复验。

**FR-PKG-3（P1）安装引导**
- AC1 各出口按其等级提供对应安装引导；未验证出口注明"实验包，可能无法导入"。

### 4.5 FR-QA 质量校验

**FR-QA-1（P0）分层强制关卡**
- AC1 Gate 顺序与阻断规则按 `docs/03`；关键普通文字对比度 **≥4.5**（渲染后采样：solid 直接算，渐变文字框内 ≥3×3 网格取最差，位图用最终合成像素）。
- AC2 交互区不被装饰遮挡；资产规格合规（尺寸/格式/命名/引用闭合 100%）。
- AC3 四出口一致性：设计 token 一致为硬项；平台允许差异须由能力矩阵解释并进 diff。

**FR-QA-2（P0）失败自动修正**
- AC1 QA error 先自动修正一次（产生新版本节点与 diff）；再失败才暴露并说明原因。❌ 未过自检的半成品推给用户。

**FR-QA-3（P1）深浅双模式 [调研]**
- AC1 两变体独立通过可读性关卡；❌ 简单反相。
- AC2 预览一键切换；出口按平台机制携带（不支持时两包并明确告知）。
- AC3 反馈作用于当前变体，另一变体同步并在变更摘要告知。

### 4.6 FR-PREVIEW 实时预览（核心）

**FR-PREVIEW-1（P0）真实交互运行时**
- AC1 Web 内真实按键/点击响应，当前版本资源渲染。❌ 静态图/录屏/截图轮播。
- AC2 落地文本框实际打出文字。

**FR-PREVIEW-2（P0）真实拼音引擎**
- AC1 任意拼音产出真实候选（词库以 PINYIN-001 benchmark 为准；未达标前 UI 标注"简化预览词库"）。❌ 写死候选词。
- AC2 按键→候选 ≤50ms（P95，1000 次，固定环境，见 `docs/03` §13）。

**FR-PREVIEW-3（P0）操作场景覆盖**
- AC1 26 键 + 候选选择（数字/点击/翻页）；AC2 中英切换；**AC3 符号/数字面板切换（当前未接通，UX-001）**；AC4 候选翻页动效；AC5 按键按下态；AC6 状态栏各状态；AC7（P1）横竖排候选（PC）。

**FR-PREVIEW-4（P0）平台差异**
- AC1 预览按 outlet 加载 PreviewProfile（候选行为/功能栏/能力差异至少一项真实可感）。❌ 一套逻辑打两个平台皮（当前状态即此违规项，PRE-001 修复）。

**FR-PREVIEW-5（P0）秒级版本切换**：历史版本预览切换 ≤1s（P95）。

**FR-PREVIEW-6（P0）移动（Android）运行时**
- AC1 真实 T9 候选。❌ 26 键套壳；AC2 九宫格/26 键一键切换；AC3 手机框 + 落地文本；AC4 按键音效（≤30ms 触发）；AC5 按下动效 ≥50fps；AC6 长按/上滑；AC7 振动尽力还原并**明示模拟边界**；AC8 候选横向翻页 + 面板；AC9 三档 DPI 真实资源选择（当前 CSS 缩放为过渡态）；AC10 ≤50ms 跟手；AC11 搜狗/百度 Android 各自 profile。

**FR-PREVIEW-7（P1）试打/标注模式分离（UX-002）**
- AC1 试打模式不被标注层拦截；标注模式点击返回 element_id；移动端不依赖右键/Alt。

### 4.7 FR-FEEDBACK 反馈迭代

**FR-FEEDBACK-1（P0）两种形式一等支持**：自然语言 + 元素标注（点选/框选映射 element_id 集合）。❌ 只支持点选。

**FR-FEEDBACK-2（P0）分类与精准路由**
- 具体参数→token 层；布局→A2；整体风格→A1（保留满意元素）；**平台特定→只改对应 TargetVariant（token diff 证明，当前未实现，PIPE-001）**；交互→预览参数层。❌ 一句话=全部重跑。

**FR-FEEDBACK-3（P0）变更最小化**
- AC1 显式满意标记为硬保护；"两轮未提及"仅作软偏好（可见可撤销）。
- AC2 未选中区域**参数级不变**由 token diff 证明（像素级为后续增强）。

**FR-FEEDBACK-4（P0）闭环可感知**：变更摘要/前后对比展示改了什么。

**FR-FEEDBACK-5（P1）圈选 + 幅度隐喻 [调研]**：选区大小对应改动幅度；一次反馈一个焦点；未选区不变（diff 证明）。

### 4.8 FR-VER 版本管理

- AC1 版本树可查看/预览/分叉；AC2（P1）元素级合并（结构化 element path + MergeRecord）；AC3 **确认此版本才可正式导出**（UX-003），fork 回 draft。❌ 系统自作主张定稿；❌ 每轮追问"还满意吗"。

### 4.9 FR-EXPORT 导出

- AC1 按出口独立导出 + 安装引导；AC2（P1）可读性问题一键修复（作为版本节点）；AC3 未确认版本仅实验导出且 `experimental=true`；AC4 跨出口一致性按 FR-QA-1。

### 4.10 FR-SHARE / FR-LEARN（P1/P2）

- FR-SHARE-1（P1）项目文件导出/导回完整还原；不泄露未授权素材与密钥。FR-SHARE-2（P2）模板/IP 库（需授权审计）。
- FR-LEARN-1（P2）同项目偏好可查看/清除；跨项目需显式授权；**未注入生成链路前不得宣称"已学习"**（当前仅展示）。

### 4.11 FR-LLMAPI LLM 与自动化 API（P1，可选扩展）

- AC1 无模型时全功能可运行（确定性降级），降级状态如实展示。
- AC2 密钥默认不持久化（会话级）；浏览器直连模式明示风险；项目导出不含密钥（ADR-005）。
- AC3 服务端对自定义 endpoint/图片 URL 实施协议/私网/重定向/大小校验（SSRF 防护）。
- AC4 非 loopback 部署强制认证；job 不可枚举 ID、有 TTL/并发上限。

### 4.12 FR-UX 前端体验

- AC1 预览是主角；反馈区常驻不遮挡。AC2 分出口独立进度（JOB-001），❌ 统一"生成中"。AC3 设备×厂商二维切换随时可见当前目标。AC4 反馈像聊天一样低门槛。

---

## 5. 非功能需求（NFR）

| ID | 需求 | 验收标准 |
|---|---|---|
| NFR-PERF-1 | 预览跟手 | 按键→候选 ≤50ms（P95/1000 次/固定环境） |
| NFR-PERF-2 | 版本切换 | ≤1s（P95） |
| NFR-PERF-3 | 音效 | ≤30ms 触发（P95） |
| NFR-PERF-4 | 动效 | ≥50fps（5s 序列，掉帧率并记） |
| NFR-PERF-5 | 局部重跑 | 显著快于全量（固定 fixture 对比） |
| NFR-REL-1 | 可靠性 | 失败明确报告卡点；❌ 静默假产物 |
| NFR-SEC-1 | 密钥 | 不入 localStorage 默认路径/日志/项目文件 |
| NFR-SEC-2 | 网络 | SSRF 防护 + 大小/超时限制（服务端） |
| NFR-SEC-3 | 版权 | 不绕过平台保护；素材授权可审计 |
| NFR-OBS-1 | 可观测 | 分出口成功率/耗时/降级率/QA 结果/反馈轮次/未选区漂移率 |
| NFR-MAINT-1 | 兼容 | 出口 E4 证据绑定客户端版本；升级复验 |

---

## 6. 数据需求

按 `docs/01-领域模型与数据契约.md` 执行（Project/Version/DesignSnapshot/AssetBundle/TargetVariant/OutletJob/ChangeInstruction/PackageArtifact/QAReport/Provenance；schemaVersion + 迁移）。

---

## 7. 验收总纲

一个版本/出口被判合格，须同时满足：

1. 对应交付等级的全部 Gate 报告通过（`docs/03` §3 映射）；
2. 无任何 ❌ 实现被采用；
3. NFR 达标（含统计口径与环境记录）；
4. 关键路径经**真实交互**验证；`install_verified` 必须有真机 Evidence（截图/录屏仅为附件，不能替代安装事实）；
5. 状态升级引用 Evidence ID，且在 `docs/02` 平台矩阵同步。

---

## 8. 追溯矩阵

| 需求域 | 条款 | 支撑文档/任务 |
|---|---|---|
| 输入 | FR-INPUT-1..4 | UX-004、refMedia 边界 |
| Brief | FR-BRIEF-1..2 | UX-003（Brief hash） |
| 生成/资产 | FR-GEN-1..3 | DOM-001/002、ASSET-001 |
| 打包/出口 | FR-PKG-1..3 | `docs/02`、OUT-* 任务 |
| QA | FR-QA-1..3 | QA-001、ADR-004/008 |
| 预览 | FR-PREVIEW-1..7 | UX-001/002、PRE-001、MOB-001..004 |
| 反馈 | FR-FEEDBACK-1..5 | PIPE-001、ADR-003 |
| 版本/导出 | FR-VER/FR-EXPORT | UX-003、JOB-001 |
| LLM/API | FR-LLMAPI | SEC-001..003、ADR-005 |
| NFR | 全部 | ENG-001、OBS-001、`docs/03` §13/14 |
