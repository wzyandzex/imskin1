# IMSkin 项目状态与交付说明（Handoff 快照）

> 快照时间：2026-08-16（第四批任务收口：ASSET-001 / OUT-SGPC-001 / QA-001 / SEC-001..003 / ENG-001a / EVID-001a）
> 面向：接手开发/评审
> 上游依据：`docs/00-总规划与开发基线.md`（等级定义）、`docs/02-平台能力矩阵与适配契约.md`（出口事实）、`docs/05-风险登记与证据包规范.md`（风险台账）
> 仓库：github.com/wzyandzex/imskin1（main）；任务状态见 `docs/06` §11
>
> 本文件是**动态快照**：每个任务批次结束后更新；历史版本见 `docs/archive/2026-08-15/`，不在此重复维护旧统计数字。

## 0. 一页速览

**是什么**：输入法皮肤生成与真实交互预览工作台（原型/MVP）。想法 → Brief 确认 → 生成 → 真实试打 → 自然语言反馈 → 版本树 → 确认定稿 → 按出口导出。

**当前总体判定**：

- 源码与单测基线完整（统计以 `npm test` 命令输出为准）；
- 四出口适配器均为 **`structural`**（结构可生成，有包结构测试）；
- **没有任何出口完成真实客户端安装验证**；默认导出以配置骨架为主；
- 平台差异预览、符号/数字面板、定稿门禁、四出口独立进度存在已知实现缺口（§3）。

**启动**：`npm run dev -w @imskin/web-app` → http://localhost:5173/

## 1. 出口交付状态（权威表以 docs/02 为准）

| Outlet | 格式 | 代码实现 | 包结构测试 | 平台专属预览 | 资产完整 | 真机安装 | 等级 |
|---|---|:-:|:-:|:-:|:-:|:-:|---|
| sogou_pc | .ssf | ✅ | ✅ | ❌ | ❌ | 未验证 | `structural` |
| sogou_android | .ssf | ✅ | ✅ | ❌ | ❌ | 未验证 | `structural` |
| baidu_pc | .bps | ✅ | ✅ | ❌ | ❌ | 未验证 | `structural` |
| baidu_android | .bds | ✅ | ✅ | ❌ | ❌ | 未验证 | `structural` |

**"structural ≠ 可安装"**。等级定义见 `docs/00` §1。

## 2. 能力现状（按用户旅程）

| 能力 | 源码 | 自动化测试 | 浏览器实测 | 备注 |
|---|:-:|:-:|:-:|---|
| 想法/参考图/视频输入 | ✅ | ✅ | 部分 | 视频仅静态关键帧；动效意向未路由 |
| 一次追问 + Brief 确认（推断高亮） | ✅ | ✅ | 部分 | Brief 字段展示不全（材质/动效未呈现） |
| 生成 + 可读性自检 | ✅ | ✅ | — | 对比度 ≥4.5 有性质测试 |
| PC/手机真实试打（拼音/T9/音/手势） | ✅ | ✅ | 部分 | 符号/数字面板已接通（UX-001 ✅）；**试打/标注模式已分离（UX-002 ✅，试打零拦截）** |
| 平台(搜狗/百度)切换 | ✅ | ✅ | 部分 | **预览已接 outlet profile（PRE-001 ✅）**：状态条差异 + 模拟项标注；布局/候选行为差异随 MOB-004 深化 |
| 反馈分类/路由/定向修改/fork | ✅ | ✅ | 部分 | 提交健壮性（UX-004 ✅）；**平台定向重跑已落地（PIPE-001 ✅）**：厂商点名反馈只改该厂商两出口（字节级证明），通用反馈清空覆盖四端同步 |
| 版本树/对比/项目导出导回 | ✅ | ✅ | 部分 | 定稿门禁（UX-003 ✅）；**深层不可变 + 点路径合并 + 坏快照拒绝（DOM-002 ✅）** |
| 四出口导出 | ✅ | ✅ | 部分 | **单出口隔离 + 结构报告 + 交付等级闸门（JOB-001/OUT-SGPC-001/QA-001 ✅）**：导出面板有四出口状态卡（等级+缺口）；**sogou_pc 确认后达 install_candidate（A3-001 ✅：确定性资产闭合）**，其余出口校验器 not_run |
| 深浅双模式/DPI 预览 | ✅ | ✅ | 部分 | DPI 为 CSS 缩放模拟（MOB-003 待做） |
| 移动面板/手势/触感 | ✅ | ✅ | 部分 | 表情面板 + 候选滑动翻页 + 振动降级（MOB-001/002 ✅）；swipe 输入未做（profile 支持时） |
| 偏好学习 | 部分 | 部分 | — | 仅展示，未注入生成链路 |
| 四出口独立进度 | 部分 | ✅ | — | 同步分出口状态已接（JOB-001 ✅）；真异步 stage/进度待图像生成接入 |
| LLM 接入（A1/A5 增强） | ✅ | ✅(mock) | 部分 | **URL 边界已收口（SEC-002 ✅：https/回环 http/拒内网与 metadata）；存储边界已明示（SEC-003 ✅）** |
| 真机安装验证 | ❌ | ❌ | ❌ | **工具就绪（EVID-001a ✅：evidence CLI + 指引）；执行需真实客户端（EVID-001b，用户操作）** |

## 3. 当前 P0 缺口（任务定义见 docs/06）

第一～四批程序侧已收口（契约/门禁/安全/类型全绿）。剩余：

1. **EVID-001b 真机执行**：装一次真实搜狗 PC 客户端跑七场景（指引 docs/evidence/outlets/sogou-pc/README.md，用户操作）；
2. **A3 位图管道**：图像生成产物注册为 AssetDescriptor——当前必需位图（状态栏图标）缺口是 install_candidate 的真实闸门；
3. **PINYIN-001（剩余）**：词库选型替换（日常 60 词 8.3% 命中已量化，需外部数据源+许可审查）；
4. **G5 一致性阈值校准（R-10）/ G6 Chromium E2E（ENG-001b）**：previewable 及以上等级的授予依据；
5. **MOB-003/004**：DPI 真实化与 Android 平台 profile 深化；
6. **其余三出口结构校验器**（随各自字段收口接入，当前 not_run）。

## 4. 受阻项与解封条件

| 缺口 | 阻因 | 解封动作 | 解封后改动点 |
|---|---|---|---|
| 真机安装（四出口） | 无真实客户端环境 | 按 `docs/05` §6 做安装验证 | 各 adapter 字段字典 + 平台矩阵升级 |
| 真实位图资产 | 依赖图像生成 endpoint | ASSET-001 落地 + 用户/服务配图 | exportSkinSet 资产驱动 |
| 百度 PC 压缩方式 | store vs deflate 未知 | 双包对照实验（R-02） | bps.ts 打包层 |
| 百度 Android Token | MD5 输入语义未知 | 候选实验矩阵（R-03） | info.ts emitToken |
| 搜狗 Android 布局键名 | 无真实模板样本 | 样本核对（R-04） | layout.ts |
| 词库覆盖率 | 未选型未建基准 | PINYIN-001 | dict.ts 替换 |

## 5. 已知事实澄清（替代旧文档矛盾表述）

- ~~"v1.2 新条款全部未实现"~~ → 实际部分已实现（见 §2），以逐项状态为准；
- ~~"13 个包"~~ → 14 个 workspace；
- ~~"203 / 305 个测试"~~ → 统计以命令输出为准（GOV-002 统一脚本落地前不再手写数字）；
- ~~"百度 PC 必须驱动官方编辑器"~~ → 已由逆向证据改为编程直生，但真机验证未完成；
- "字段经逆向确证" ≠ "客户端可安装"——前者是 E2 级证据，后者需 E4 级。

## 6. 下一步（第五批后）

```text
用户侧：EVID-001b 真机执行（搜狗 PC 七场景——sogou_pc 已达 install_candidate，装一次即可升 install_verified）
开发侧：PINYIN-001 词库接入（调研完成后选型替换，复测 benchmark）
        ENG-001b Chromium E2E 骨架 → previewable 授予依据
        A3-002 键盘背景位图（LLM 图像 → AssetDescriptor 注册 → 九宫格渲染联动）
```

基线：待批次收尾更新；`npm run verify` = typecheck + test + build。

## 7. 多轮审查记录（历史）

- Workflow #1（建包）：核心包构建 + 对抗性验证，修复 qa 渐变误判、INI 注入 2 缺陷；
- Workflow #2（硬化）：修复 session 标点错序、xi'an 分隔符、A1 mood 冻结、reset 旧快照复活 4 bug + 2 组件隐患；
- Workflow #3（逆向落地）：四出口格式按逆向笔记落地适配层；
- Workflow #4（2026-08-15 规划基线）：三轮多 agent 审查（产品/UX/工程）+ 总规划文档体系落定（docs/00-06 + ADR-001..008 + 归档）；
- Workflow #5（2026-08-16 第一批任务）：Git 基线推送（`0d82120`）；UX-001 面板接通（`f63995a`）；UX-004 反馈健壮性（`2d97fe2`）；PINYIN-001 基准 harness + initial 证据（`acec0a5`，量化 R-09 缺口：引擎正确性 98.1% vs 日常命中 8.3%）。
- Workflow #6（2026-08-16 第二批任务）：DOM-001 contracts 包（`8238dcd`）；PRE-001 预览接 outlet（`b47d331`）；UX-002 试打/标注分离（`2d3d181`）；UX-003 确认门禁 + CLI 实验包标注（`c27081f`）；UX-005 窄屏抽屉/dialog 语义/tab/live region（`a22d322`）。基线 339 测试全绿。
- Workflow #7（2026-08-16 第三批任务）：DOM-002 深层不可变 + 点路径合并 + 快照校验（`ba1dca5`）；JOB-001 单出口隔离导出 + 分出口状态（`f1cf986`）；MOB-001 表情面板 + 候选滑动翻页（`9934147`）；MOB-002 按键触感诚实降级（`b942735`）；PIPE-001 平台定向重跑——"百度这边…"字节级证明只改百度两出口（`f2b68ae`）。基线 348 测试全绿。
- Workflow #8（2026-08-16 第四批任务）：ASSET-001 资产契约 + G2 缺口诚实列示（`3af0d81`）；OUT-SGPC-001 validateSsf 结构校验（`2b3d0b0`）；QA-001 交付等级闸门 + 出口状态卡（`09fc237`）；SEC-001/002 API 加固 + SSRF 守卫（`1dc1f76`）；SEC-003 密钥边界（`184059f`）；ENG-001a 全仓 strict 类型门禁（`286b435`）；EVID-001a 证据 CLI + 真机指引（`239d486`）。基线 374 测试全绿。
